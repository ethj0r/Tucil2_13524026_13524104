\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{geometry}
\usepackage{setspace}
\usepackage{parskip}

\geometry{
    left=2.5cm,
    right=2.5cm,
    top=2.5cm,
    bottom=2.5cm
}
\onehalfspacing

\usepackage{graphicx}
\usepackage{float}
\usepackage{subcaption}
\graphicspath{{./}{./assets/}{./images/}}

\usepackage{tikz}
\usetikzlibrary{shapes.geometric, shapes.misc, arrows.meta, positioning, calc}

\usepackage{listings}
\usepackage{xcolor}

\definecolor{codebg}{HTML}{F7F7F7}
\definecolor{codeframe}{HTML}{DDDDDD}
\definecolor{keyword}{HTML}{005FAF}
\definecolor{comment}{HTML}{6A737D}
\definecolor{string}{HTML}{D73A49}

\lstset{
    basicstyle=\ttfamily\footnotesize,
    keywordstyle=\color{keyword}\bfseries,
    commentstyle=\color{comment}\itshape,
    stringstyle=\color{string},
    numbers=left,
    numberstyle=\tiny\color{gray},
    backgroundcolor=\color{codebg},
    frame=single,
    rulecolor=\color{codeframe},
    breaklines=true,
    tabsize=4,
    showstringspaces=false,
    language=C++,
    aboveskip=1em,
    belowskip=1em
}

\usepackage{amsmath}
\usepackage{amssymb}

\usepackage{booktabs}
\usepackage{tabularx}

\AddToHook{cmd/section/before}{\clearpage}

\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=keyword,
    citecolor=black,
    pdfauthor={Made Branenda Jordhy, Valentino Daniel Kusumo},
    pdftitle={3D Voxelization using Octree}
}

%HEADER/FOOTER
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\setlength{\headheight}{36pt}
\fancyhead[L]{%
    \small\textnormal{School of Electrical Engineering and Informatics}\\[-1pt]
    \small\textnormal{Institut Teknologi Bandung}%
}
\fancyhead[R]{\includegraphics[height=1cm]{assets/LogoITB.png}}
\fancyfoot[C]{\small\textnormal{IF2211 Strategi Algoritma}}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.2pt}

% TITLE
\title{
    \vspace{-1.5cm}
    {\Large\textsc{Tugas Kecil II --- IF2211 Strategi Algoritma}}\\[0.4cm]
    {\LARGE\textbf{3D Voxelization using Octree}}\\[0.3cm]
    \vspace{-0.3cm}
}
\author{}
\date{\today}

\begin{document}

\maketitle
\thispagestyle{empty}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{testcase/sphere_aft.png}
\end{figure}

\vspace{1cm}
\begin{center}
    \textbf{Made Branenda Jordhy} --- 13524026 \\[0.15cm]
    \textbf{Valentino Daniel Kusumo} --- 13524104
\end{center}

\vspace{4cm}
\begin{center}
    {\large\textbf{School of Electrical Engineering and Informatics}}\\[0.1cm]
    {\large\textbf{Institut Teknologi Bandung}}\\[0.1cm]
    {\large\textbf{Semester II 2025/2026}}\\[0.1cm]
\end{center}

\newpage
\tableofcontents
\newpage

%INTRODUCTION
\section{Introduction}
Dalam \textit{computer graphic}, objek 3D itu umumnya disimpan sebagai \textit{polygon mesh}.
Format ini fleksibel dan ringan tetapi tidak selalu cocok untuk kebutuhan tertentu.
Seperti yang kita tau, \textit{grid based physics simulations}, \textit{3D printing}, hingga
game seperti Minecraft, itu semuanya membutuhkan representasi volumetrik yang lebih terstruktur.
Di sinilah proses \textit{voxelization} digunakan yaitu mengubah \textit{polygon mesh} menjadi
kubus kecil seragam yang disebut \textit{voxel}.

Problem utamanya adalah kita diberikan sebuah file \texttt{.obj} berisi \textit{polygon mesh}
dan program harus menghasilkan \texttt{.obj} baru yang mana seluruhnya tersusun dari voxel
yang seragam yang merpresentasikan objek aslinya. Namun, ada obstacle yang terletak pada
scale, pada depth octree yang tinggi, jumlah candidate voxel cenderung tumbuh secara eksponensial
($8^N$ pada depth $N$).

Lalu bagaimana caranya kita develop? Ya, dengan menggunakan paradigma \textit{Divide and Conquer}
melalui \textit{Octree} (ya sesuai spesifikasi tucil...). Ruang 3D dibungkus oleh bounding box
(AABB) berbentuk cubic lalu dipotong secara rekursif menjadi 8 sub-cubic pada setiap level.
Di setiap step, kita melakukan uji perpotongan antara segitiga mesh dan kotak menggunakan
\textit{Separating Axis Theorem} (SAT). Kotak yang tidak bersinggungan dengan segitiga manapun
langsung di-\textit{pruned} sehingga hanya bagian yang relevan yang ditelusuri lebih dalam.
Proses ini naturally membentuk paradigma \textit{Divide and Conquer}, divide ruang, conquer
setiap bagian independently, lalu terakhir tinggal combine.

Program yang kita develop di sini sepenuhnya dalam \texttt{C++}. Selain fungsionalitas utama (wajib),
kami juga mengimplementasikan dua fitur bonus yaitu \textit{concurrency} untuk paralelisasi
rekursif pada octree menggunakan \texttt{std::thread} serta \textit{interactive 3D viewer}
dengan raylib yang memungkinkan user melakukan eksplorasi hasil voxelization.

%THEORITICAL BACKGROUND
\section{Theoretical Background}\label{sec:teori}
\subsection{Voxelization}
Secara sederhana, \textit{voxelization} adalah proses mengubah model 3D yang mulus
(biasanya \textit{polygon mesh}) menjadi kumpulan kotak - kotak kecil dalam grid 3D.
Analogi sederhananya adalah jika dunia 2D memiliki \textit{pixel}, maka dunia 3D memiliki
\textit{voxel} (\textit{volumetric pixel}) sebagai blok bangunan utamanya. Setiap voxel merepresentasikan sebuah kubus kecil
di ruang dan menyimpan informasi apakah area tersebut dianggap terisi atau kosong.

Dalam praktiknya, ada dua pendekatan utama. Pertama, \textit{surface voxelization}, yang hanya
menandai kotak - kotak pada bagian permukaan objeknya saja. Kedua, \textit{solid voxelization},
yang benar - benar mengisi sampai ke bagian dalam objeknya. Pada tugas ini, pendekatan yang digunakan
adalah \textit{surface voxelization} karena target utama kita hanya struktur bentuk luarnya saja agar lebih efisien.

Mengapa metode ini penting? Karena banyak sistem lebih mudah bekerja pada data berbasis grid,
misalnya untuk \textit{3D printing}, visualisasi permainan, dan masih banyak contoh lainnya.
\subsection{Octree}
\textit{Octree} adalah struktur data hierarkis untuk partisi ruang 3D. Satu node merepresentasikan
sebuah \textit{axis-aligned bounding box} (AABB) berbentuk kubus, lalu node tersebut dibagi menjadi
8 child kubus yang ukurannya setengah pada masing-masing sumbu. Proses pembagian ini bisa dilakukan
berulang sampai kedalaman tertentu.

Kelebihan utama Octree adalah kemampuannya melakukan \textit{adaptive refinement}. Area ruang yang
akan digunakan (misalnya dekat permukaan objek) dapat dibagi lebih dalam, sedangkan area kosong dapat
dipangkas lebih awal. Hal ini jauh lebih efisien dibanding membuat seluruh grid \textit{uniform} dari awal,
terutama saat model hanya menempati sebagian kecil ruang.

Tentu ada \textit{trade-off} antara kedalaman dan resolusi. Semakin dalam tingkat Octree-nya, voxel-nya
akan semakin kecil dan detail objek akan semakin tajam. Namun, jumlah sel dapat meningkat secara drastis.
Oleh karena itu, strategi \textit{pruning} (pemangkasan) wajib untuk diimplementasikan 
agar komputasi tetap efisien. 

\subsection{Divide and Conquer}
Algoritma \textit{Divide and Conquer} bekerja melalui tiga tahap: \textit{divide}, \textit{conquer},
dan \textit{combine}. Pertama, masalah besar dipecah menjadi submasalah yang lebih kecil. Kedua,
setiap submasalah diselesaikan secara independen (\textit{umumnya rekursif}). Ketiga, hasil
submasalah digabung untuk membentuk solusi akhir.

Pada kasus voxelization berbasis Octree, pola ini muncul secara alami. Tahap \textit{divide}
dilakukan saat satu kubus dibagi menjadi 8 sub-kubus. Tahap \textit{conquer} dilakukan dengan
mengecek masing-masing sub-kubus: apakah perlu dibagi lagi, dipangkas, atau dijadikan voxel akhir.
Tahap \textit{combine} terjadi ketika seluruh hasil voxel dari subtree dikumpulkan menjadi satu kesatuan model yang utuh.

Keuntungan pendekatan ini adalah struktur rekursifnya bersih, modular, dan sangat cocok untuk
dibuat paralel. Karena banyak subtree saling independen, proses \textit{conquer} dapat dijalankan
bersamaan menggunakan \textit{thread} terpisah jika diperlukan (\textit{fitur bonus}).
\subsection{Separating Axis Theorem (SAT)}
Inti dari voxelization permukaan adalah untuk mengecek
\textit{apakah sebuah segitiga mesh berpotongan dengan sebuah AABB?}
Untuk itu, kita menggunakan \textit{Separating Axis Theorem} (SAT).

Prinsip SAT menyatakan bahwa dua objek konveks (\textit{himpunan titik atau bentuk geometri yang tidak memiliki lekukan ke dalam}) 
tidak berpotongan jika dan hanya jika ada
setidaknya satu sumbu proyeksi yang memisahkan keduanya (\textit{separating axis}). Jadi,
untuk membuktikan dua objek berpotongan, kita cukup memastikan bahwa tidak ada sumbu pemisah.

Pada pengujian segitiga vs AABB, himpunan sumbu uji yang umum dipakai berjumlah 13, yaitu
3 sumbu normal AABB (sumbu $x$, $y$, $z$),
1 sumbu normal bidang segitiga,
dan 9 sumbu hasil \textit{cross product} antara 3 edge segitiga dengan 3 sumbu AABB.
Jika pada semua sumbu tersebut proyeksi kedua objek saling overlap, maka segitiga
dan AABB dinyatakan berpotongan.

%DIVIDE AND CONQUER
\section{Divide and Conquer Algorithm}\label{sec:algo}

Sekarang kita masuk ke inti dari project ini. Bagaimana \textit{Divide and Conquer} benar-benar
bekerja di dalam proses voxelization. Untuk SAT test-nya sendiri, kita mengacu pada paper
Akenine-M\"oller \cite{akenine2001}
yang memang jadi standar untuk triangle-AABB intersection. Sedangkan konsep octree-based
voxelization-nya terinspirasi dari Schwarz dan
Seidel \cite{schwarz2010},
meskipun paper mereka target-nya GPU, kita adapt untuk CPU secara sequential
(plus concurrency sebagai bonus).

\subsection{High-Level Steps}

Pertama tama, bungkus seluruh model dalam satu kotak besar, lalu potong-potong
secara rekursif. Di setiap level, lakukan pengecekan ``ada segitiga mesh yang menyentuh kotak ini
tidak?'' Kalau tidak ada, buang (pruned). Kalau ada dan sudah di level terdalam, itu voxel. Kalau ada
tapi belum cukup dalam, potong lagi jadi 8 child.

Untuk lebih detail, prosesnya seperti berikut ini.

\begin{enumerate}
    \item \textbf{Bungkus model dalam bounding box.}
    Pertama, kita hitung AABB terkecil yang membungkus semua vertex. Lalu kita normalisasi
    jadi kubus sempurna, pakai sisi terpanjang sebagai acuan. Kenapa harus kubus sempurna?
    Karena kalau tidak, voxel di leaf level ukurannya tidak seragam, dan hasilnya jadi aneh.

    \item \textbf{Filter segitiga pakai SAT.}
    Untuk kotak saat ini, kita loop semua segitiga dan cek satu per satu: ``segitiga ini
    berpotongan dengan kotak ini?'' Cek-nya pakai SAT dengan 13 sumbu uji. Yang lolos
    kemudian dikumpulkan di list \texttt{intersecting}, sisanya tidak perlu diteruskan ke level
    berikutnya.

    \item \textbf{Pruning. Kotak kosong langsung dibuang.}
    Jika \texttt{intersecting}-nya kosong, berarti tidak ada permukaan objek di area ini.
    Node langsung di-mark sebagai \textit{pruned}, rekursi stop. Mayoritas ruang 3D itu
    kosong, jadi mayoritas node itu di-prune.

    \item \textbf{Leaf node. Save sebagai voxel.}
    Jika depth sudah sama dengan \texttt{maxDepth} dan masih ada segitiga yang menyentuh,
    berarti kotak ini valid sebagai voxel. Langsung push ke \texttt{result.voxels}.

    \item \textbf{Divide. Split jadi 8 child.}
    Jika belum \texttt{maxDepth} dan masih ada segitiga, kotak dipotong jadi 8 sub-kubus.
    Dengan cara split titik tengah di setiap sumbu (X, Y, Z). Di implementasi, kita pakai
    bit manipulation, index 0--7 menentukan kombinasi half-space di tiap sumbu.

    \item \textbf{Conquer. Rekursi tiap child.}
    Setiap child di-handle independently, masing-masing cuma nerima segitiga yang sudah
    difilter (bukan seluruh mesh). Proses berulang dari step 2. Karena tiap subtree
    itu saling independen, ini juga yang bikin concurrency-nya natural.

    \item \textbf{Combine. Kumpulkan semua voxel.}
    Setelah semua cabang rekursi selesai, semua leaf node dari setiap subtree terkumpul
    jadi satu list. List itulah yang akhirnya kita export jadi file \texttt{.obj}.
\end{enumerate}

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=1.2cm and 0.5cm,
        startstop/.style={rounded rectangle, draw, fill=codebg, minimum width=3.2cm,
            minimum height=0.8cm, font=\small\ttfamily},
        process/.style={rectangle, draw, fill=codebg, minimum width=3.8cm,
            minimum height=0.8cm, font=\small},
        decision/.style={diamond, draw, fill=yellow!12, minimum width=2.2cm,
            minimum height=1cm, font=\small, aspect=2.2, inner sep=1pt},
        result/.style={rectangle, draw, fill=green!15, minimum width=3cm,
            minimum height=0.8cm, font=\small, rounded corners=3pt},
        pruned/.style={rectangle, draw, fill=black!8, minimum width=3cm,
            minimum height=0.8cm, font=\small, rounded corners=3pt},
        arr/.style={-{Stealth[length=2.5mm]}, thick},
        lbl/.style={font=\footnotesize, fill=white, inner sep=1.5pt}
    ]
        %Nodes
        \node[startstop] (start) {buildOctree(box, tri, d)};
        \node[process, below=of start] (filter) {Filter segitiga: SAT test};
        \node[decision, below=1.3cm of filter] (empty) {\shortstack{intersecting\\kosong?}};
        \node[decision, below=1.3cm of empty] (leaf) {\shortstack{depth ==\\maxDepth?}};
        \node[process, below=1.3cm of leaf] (split) {Split box $\to$ 8 child};
        \node[startstop, below=of split] (recurse) {buildOctree(child, ...)};

        %Side results
        \node[pruned, right=2.5cm of empty] (prune) {Pruned (stop)};
        \node[result, right=2.5cm of leaf] (voxel) {Simpan sebagai voxel};

        %Arrows
        \draw[arr] (start) -- (filter);
        \draw[arr] (filter) -- (empty);
        \draw[arr] (empty) -- node[lbl, left] {Tidak} (leaf);
        \draw[arr] (empty) -- node[lbl, above] {Ya} (prune);
        \draw[arr] (leaf) -- node[lbl, left] {Tidak} (split);
        \draw[arr] (leaf) -- node[lbl, above] {Ya} (voxel);
        \draw[arr] (split) -- node[lbl, left] {$\times\,8$} (recurse);

        %Loop back
        \draw[arr, dashed] (recurse.west) -- ++(-1.8,0) |- (filter.west);
    \end{tikzpicture}
    \caption{Flow chart keputusan pada setiap level rekursi \texttt{buildOctree}.}
    \label{fig:flowchart-octree}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.22\textwidth}
        \includegraphics[width=\textwidth]{assets/subdiv-step1.png}
        \caption{Bounding box awal membungkus seluruh objek.}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \includegraphics[width=\textwidth]{assets/subdiv-step2.png}
        \caption{Dibagi 4 (2D). Kotak kosong di-prune (abu-abu).}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \includegraphics[width=\textwidth]{assets/subdiv-step3.png}
        \caption{Subdivisi lanjutan pada kotak yang tersisa.}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.22\textwidth}
        \includegraphics[width=\textwidth]{assets/subdiv-step4.png}
        \caption{Level terdalam: kotak hijau = voxel akhir.}
    \end{subfigure}
    \caption{Ilustrasi 2D (quadtree) dari proses subdivisi bertahap. Pada 3D,
    setiap kotak dibagi menjadi 8 (bukan 4), tetapi prinsipnya identik.}
    \label{fig:subdivision-steps}
\end{figure}

\subsection{Pseudocode}

Pseudocode di bawah ini merepresentasikan fungsi \texttt{buildOctree} yang jadi core dari
seluruh proses. Fungsinya cukup compact, terima kotak, daftar segitiga, depth saat ini,
dan depth maksimum, lalu bangun octree secara top-down.

\begin{figure}[H]
\begin{lstlisting}[language={},morekeywords={function,for,each,in,if,then,return,end},
    title={\textbf{BuildOctree}, Rekursi Divide and Conquer},
    label={alg:buildoctree}]
function BuildOctree(box, triangles, depth, maxDepth):
    // Input:  box (AABB), triangles (daftar segitiga),
    //         depth (kedalaman saat ini), maxDepth (kedalaman maks)
    // Output: leaf nodes (voxel) ditambahkan ke result

    nodesPerDepth[depth] += 1

    // Filter: hanya simpan segitiga yang menyentuh kotak ini
    intersecting = []
    for each tri in triangles:
        if IntersectsTriangleBox(tri, box) then
            intersecting.append(tri)
        end if
    end for

    // Pruning: tidak ada segitiga -> kotak kosong
    if intersecting is empty then
        prunedPerDepth[depth] += 1
        return
    end if

    // Base case: kedalaman maksimum tercapai -> voxel
    if depth == maxDepth then
        result.voxels.append(box)
        return
    end if

    // Divide: potong kotak menjadi 8 sub-kubus
    children = SplitBox(box)

    // Conquer: rekursi ke setiap child secara independen
    for each child in children:
        BuildOctree(child, intersecting, depth + 1, maxDepth)
    end for
end function
\end{lstlisting}
\end{figure}

\texttt{IntersectsTriangleBox} itu SAT test lengkap 13 sumbu yang
sudah dibahas di Bagian~\ref{sec:teori}. Dan \texttt{SplitBox} tinggal bagi kotak jadi 8
pakai midpoint tiap sumbu. Satu hal penting, di step \textit{Conquer}, tiap child cuma
nerima segitiga yang sudah difilter (\texttt{intersecting}), bukan seluruh mesh. Jadi semakin
dalam level-nya, semakin sedikit segitiga yang perlu dicek.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        level distance=2cm,
        sibling distance=1.2cm,
        every node/.style={circle, draw, minimum size=6mm, font=\scriptsize\bfseries, inner sep=0pt},
        internal/.style={fill=white},
        pruned/.style={fill=black!15, text=black!50},
        voxel/.style={fill=green!30},
        edge from parent/.style={draw, -{Stealth[length=2mm]}, thin},
        level 1/.style={sibling distance=3.2cm},
        level 2/.style={sibling distance=0.9cm},
    ]
        \node[internal] (root) {R}
            child { node[internal] (n1) {1}
                child { node[voxel] (leaf0) {v} }
                child { node[pruned] {$\times$} }
                child { node[voxel] {v} }
                child { node[pruned] {$\times$} }
            }
            child { node[pruned] {$\times$} }
            child { node[internal] (n3) {3}
                child { node[pruned] {$\times$} }
                child { node[voxel] {v} }
                child { node[voxel] {v} }
                child { node[voxel] {v} }
            }
            child { node[pruned] {$\times$} }
        ;

        %Depth labels
        \node[draw=none, fill=none, font=\normalsize, anchor=east]
            at ([xshift=-1.2cm]leaf0.west |- root) {depth 0};
        \node[draw=none, fill=none, font=\normalsize, anchor=east]
            at ([xshift=-1.2cm]leaf0.west |- n1) {depth 1};
        \node[draw=none, fill=none, font=\normalsize, anchor=east]
            at ([xshift=-1.2cm]leaf0.west |- leaf0) {depth 2};

        %Legend
        \matrix[draw=none, fill=none, below=-1.5cm of current bounding box.south,
            column sep=8pt, font=\small, anchor=north] {
            \node[circle,draw,fill=white,minimum size=5mm,inner sep=0pt,font=\tiny]{}; &
            \node[draw=none]{internal}; &
            \node[circle,draw,fill=black!15,minimum size=5mm,inner sep=0pt,font=\tiny]{$\times$}; &
            \node[draw=none]{pruned}; &
            \node[circle,draw,fill=green!30,minimum size=5mm,inner sep=0pt,font=\tiny]{v}; &
            \node[draw=none]{voxel}; \\
        };
    \end{tikzpicture}
    \caption{Recursion tree pada octree depth 2 (disederhchildan, 4 child per node).
    Node pruned (abu-abu) tidak ditelusuri lebih lanjut. Hanya leaf hijau yang jadi voxel akhir.}
    \label{fig:recursion-tree}
\end{figure}

\subsection{Complexity Analysis}

Misalkan $N$ = \texttt{maxDepth} dan $T$ = jumlah segitiga di mesh input.

\textbf{Worst case (tanpa pruning).}
Kalau tidak ada node yang di-prune sama sekali (kasus yang unlikely tapi tetap worth dianalisis),
setiap node punya 8 child di setiap level. Total node:
\[
\sum_{d=0}^{N} 8^d = \frac{8^{N+1} - 1}{7}
\]
Di setiap node, kita jalankan SAT test ke semua segitiga yang diteruskan. SAT test sendiri
$O(1)$ per segitiga (13 axis, operasi aritmatika konstan). Jadi worst case totalnya
$O(8^N \cdot T)$. Untuk gambaran, $N = 7$ saja itu sudah 2 juta node, kalikan dengan $T$
segitiga per node, angkanya jadi sangat besar.

\textbf{Average case (dengan pruning).}
Di praktiknya, pruning memangkas mayoritas node. Model 3D tipikal itu cuma menempati
sebagian kecil bounding box-nya, sisanya kosong. Jadi di setiap level, cuma node yang
kena permukaan objek yang ditelusuri lebih lanjut. Ditambah lagi, filtering segitiga di
tiap level juga bantu, semakin dalam, semakin sedikit segitiga yang relevan buat node
tertentu.

Jumlah voxel yang dihasilkan itu proporsional terhadap luas permukaan
objek, bukan volume bounding box. Kompleksitas aktualnya jauh di bawah worst case.

\textbf{Space complexity.}
Kita tidak menyimpan tree secara eksplisit, yang disimpan cuma leaf nodes (voxel) di
\texttt{result.voxels}. Jadi space untuk output itu $O(V)$ di mana $V$ = jumlah voxel.
Call stack rekursi memakan $O(N)$ depth, dan di setiap level ada salinan list segitiga
yang sudah difilter.

\begin{table}[H]
\centering
\caption{Pertumbuhan node maksimum vs.\ aktual (tipikal) per depth.}
\label{tab:growth}
\begin{tabular}{@{}crl@{}}
\toprule
\textbf{Depth ($N$)} & \textbf{Max nodes ($8^N$)} & \textbf{Catatan} \\
\midrule
1 & 8 & Selalu terjangkau \\
2 & 64 & Masih sangat cepat \\
3 & 512 & Umumnya $<$ 200 node aktual \\
5 & 32.768 & Pruning memangkas $>$ 80\% \\
7 & 2.097.152 & Butuh pruning \\
10 & 1.073.741.824 & Tidak praktis tanpa pruning \\
\bottomrule
\end{tabular}
\end{table}


%IMPLEMENTATION
\section{Implementation}\label{sec:impl}
\subsection{System Architecture}
Implementasi program dibagi menjadi beberapa modul sehingga tiap komponen punya tugas yang jelas:

\begin{itemize}
    \item \textbf{Parser} (\texttt{parser/ObjParser.cpp}): membaca file \texttt{.obj},
    mengekstrak vertex dan face, lalu mengubah face menjadi kumpulan segitiga
    melalui \textit{fan triangulation}.
    \item \textbf{Model/Mesh} (\texttt{model/Mesh.cpp}): menyusun data mesh,
    menghitung bounding box, dan menormalisasi bounding box menjadi kubus
    (AABB cubic) sebagai root octree.
    \item \textbf{Intersection \& Geometry} (\texttt{model/BoundingBox.cpp}):
    berisi operasi geometri dasar (\texttt{splitBox}, \texttt{centerBox}, dll.)
    serta uji \texttt{intersectsTriangleBox} berbasis SAT.
    \item \textbf{Octree} (\texttt{octree/Octree.cpp}): inti rekursi
    divide and conquer untuk membangun voxel leaf, sekaligus mengisi statistik
    node dan pruning per depth.
    \item \textbf{Exporter} (\texttt{exporter/ObjExporter.cpp}): menulis hasil voxel
    ke \texttt{output.obj}, dengan 8 vertex dan 12 face segitiga per voxel.
    \item \textbf{Statistics} (\texttt{stats/stats.cpp}): memberikan \textit{output} ringkasan metrik
    (jumlah voxel, vertex, face, distribusi node, dan waktu eksekusi).
    \item \textbf{Viewer} (\texttt{viewer/Viewer.cpp}): visualisasi interaktif voxel
    menggunakan raylib (orbit, zoom, clip slider, render mode, dll).
\end{itemize}

Secara dependensi, alur modul dapat diringkas sebagai:
\texttt{main -> parser/model -> octree -> exporter/stats -> viewer}.

\subsection{Program Flow}
Alur eksekusi program di \texttt{main.cpp} adalah sebagai berikut:

\begin{enumerate}
    \item Program membaca input path file OBJ dan nilai \texttt{maxDepth} dari user.
    \item Fungsi \texttt{loadObj()} dipanggil untuk mem-parse mesh,
    membentuk daftar segitiga, menghitung bounds, lalu membuat bounds kubik.
    \item Timer dimulai, kemudian \texttt{buildOctree()} dieksekusi dari root box
    dengan daftar segitiga awal.
    \item Setelah rekursi selesai, timer dihentikan dan waktu eksekusi dihitung.
    \item Leaf voxel diekspor ke \texttt{../test/output.obj} melalui \texttt{exportObj()}.
    \item Statistik dibangun memakai \texttt{buildStats()} dan dicetak dengan
    \texttt{printStats()}.
    \item Data voxel ditampilkan pada viewer interaktif lewat \texttt{launchViewer()}.
\end{enumerate}

Secara rekursif, \texttt{buildOctree()} melakukan proses berikut pada setiap node:

\begin{enumerate}
    \item Tambah counter node pada depth saat ini.
    \item Filter segitiga yang berpotongan dengan box saat ini
    (menggunakan \texttt{intersectsTriangleBox()}).
    \item Jika tidak ada segitiga yang berpotongan, node di-prune.
    \item Jika depth sudah mencapai \texttt{maxDepth}, box disimpan sebagai voxel leaf.
    \item Jika belum, box dibagi jadi 8 child dan diproses rekursif.
\end{enumerate}

\subsection{Implementation Details}
Beberapa keputusan implementasi penting yang memengaruhi hasil akhir:

\begin{itemize}
    \item \textbf{Fan triangulation saat parsing face.}
    Face OBJ dengan lebih dari 3 vertex dipecah menjadi beberapa segitiga.
    Pendekatan ini membuat proses berikutnya cukup menangani primitive segitiga saja.

    \item \textbf{Support indeks negatif OBJ.}
    Pada parsing face, indeks negatif tetap didukung dan di-resolve relatif terhadap
    jumlah vertex yang sudah terbaca, sehingga kompatibilitas parser lebih baik.

    \item \textbf{Normalisasi root bounds menjadi kubus.}
    Setelah bounds mesh dihitung, sisi kubus diambil dari dimensi terbesar
    (\texttt{max3(size.x, size.y, size.z)}). Hal ini penting agar subdivisi octree
    benar-benar uniform pada ketiga sumbu.

    \item \textbf{Uji SAT.}
    Sebelum SAT penuh, implementasi mengecek:
    titik segitiga di dalam box, lalu overlap antara triangle AABB dan box AABB.
    Jika gagal di tahap awal, perhitungan axis SAT tidak dijalankan.

    \item \textbf{13 sumbu SAT untuk triangle vs box.}
    Implementasi memeriksa 9 sumbu hasil \textit{cross product} edge segitiga
    dengan sumbu box, 3 sumbu utama box, dan 1 normal segitiga.

    \item \textbf{Thread pada hasil octree.}
    Struktur \texttt{OctreeResult} memakai \texttt{mutex} untuk update
    \texttt{nodesPerDepth}, \texttt{prunedPerDepth}, dan \texttt{voxels}.
    Kode sudah mendukung eksekusi paralel per subtree, namun saat ini
    \texttt{THREAD\_LIMIT} diset 1 agar eksekusi efektifnya tetap stabil.

    \item \textbf{Representasi voxel langsung sebagai kubus OBJ.}
    Setiap voxel ditulis sebagai 8 vertex dan 12 face segitiga, sehingga
    output siap diproses tool 3D standar tanpa format tambahan.

    \item \textbf{Viewer.}
    Viewer menyediakan fitur clip, wireframe, orbit camera, dan
    membantu memvalidasi (\textit{secara visual}) apakah voxelisasi permukaan sudah mengikuti bentuk mesh asli.
\end{itemize}

\section{Results and Analysis}\label{sec:hasil}
Pada bab ini, pengujian dilakukan dengan parameter depth octree tetap ($d=4$) untuk perbandingan antar-model.
Setiap pengujian menghasilkan metrik yang dicetak oleh program:
jumlah voxel, jumlah vertex/face hasil ekspor, distribusi node octree,
jumlah node yang ter-prune, serta waktu eksekusi.

Secara umum, metrik yang diamati adalah:
\begin{itemize}
    \item Kompleksitas input (jumlah vertex dan triangle mesh awal),
    \item Kompleksitas struktur octree (node aktif dan node ter-prune),
    \item Ukuran output voxel (voxel, vertex, face),
    \item Waktu komputasi (ms).
\end{itemize}

\subsection{Test Case 1}
\textbf{Model:} \texttt{cube.obj}, \textbf{depth:} 4

\begin{figure}[H]
    \centering
    \begin{minipage}[c]{0.30\textwidth}
        \centering
        \includegraphics[width=\textwidth,height=4.2cm,keepaspectratio]{testcase/cube_bef.png}\\[4pt]
        {\small (a) Mesh awal}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.30\textwidth}
        \centering
        \includegraphics[width=\textwidth,height=4.2cm,keepaspectratio]{testcase/cube_aft.png}\\[4pt]
        {\small (b) Hasil voxelization}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.34\textwidth}
        \centering
        \small
        \begin{tabular}{lr}
            \toprule
            Metrik & Nilai \\
            \midrule
            Vertex awal & 8 \\
            Triangle awal & 12 \\
            Voxel count & 1352 \\
            Vertex output & 10816 \\
            Face output & 16224 \\
            Node total & 2889 \\
            Pruned total & 1176 \\
            Waktu & 4.34 ms \\
            \bottomrule
        \end{tabular}
    \end{minipage}
    \caption{Voxelization \texttt{cube.obj} (depth 4)}
    \label{fig:tc-cube}
\end{figure}

Model kubus menghasilkan voxel count cukup tinggi karena seluruh permukaan
terdistribusi merata pada sisi-sisi AABB, sehingga banyak leaf level akhir
tetap relevan.

\subsection{Test Case 2}
\textbf{Model:} \texttt{sphere.obj}, \textbf{depth:} 4

\begin{figure}[H]
    \centering
    \begin{minipage}[c]{0.30\textwidth}
        \centering
        \includegraphics[width=\textwidth,height=4.2cm,keepaspectratio]{testcase/sphere_bef.png}\\[4pt]
        {\small (a) Mesh awal}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.30\textwidth}
        \centering
        \includegraphics[width=\textwidth,height=4.2cm,keepaspectratio]{testcase/sphere_aft.png}\\[4pt]
        {\small (b) Hasil voxelization}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.34\textwidth}
        \centering
        \small
        \begin{tabular}{lr}
            \toprule
            Metrik & Nilai \\
            \midrule
            Vertex awal & 1106 \\
            Triangle awal & 2208 \\
            Voxel count & 1160 \\
            Vertex output & 9280 \\
            Face output & 13920 \\
            Node total & 2697 \\
            Pruned total & 1200 \\
            Waktu & 7.50 ms \\
            \bottomrule
        \end{tabular}
    \end{minipage}
    \caption{Voxelization \texttt{sphere.obj} (depth 4)}
    \label{fig:tc-sphere}
\end{figure}

Meskipun voxel count sedikit lebih rendah dari kubus, waktu eksekusi lebih tinggi
karena jumlah triangle input jauh lebih besar. Hal ini menunjukkan biaya SAT
bergantung pada kompleksitas mesh sumber.

\subsection{Test Case 3}
\textbf{Model:} \texttt{cylinder.obj}, \textbf{depth:} 4

\begin{figure}[H]
    \centering
    \begin{minipage}[c]{0.30\textwidth}
        \centering
        \includegraphics[width=\textwidth,height=4.2cm,keepaspectratio]{testcase/cylinder_bef.png}\\[4pt]
        {\small (a) Mesh awal}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.30\textwidth}
        \centering
        \includegraphics[width=\textwidth,height=4.2cm,keepaspectratio]{testcase/cylinder_aft.png}\\[4pt]
        {\small (b) Hasil voxelization}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.34\textwidth}
        \centering
        \small
        \begin{tabular}{lr}
            \toprule
            Metrik & Nilai \\
            \midrule
            Vertex awal & 66 \\
            Triangle awal & 128 \\
            Voxel count & 640 \\
            Vertex output & 5120 \\
            Face output & 7680 \\
            Node total & 1801 \\
            Pruned total & 936 \\
            Waktu & 3.95 ms \\
            \bottomrule
        \end{tabular}
    \end{minipage}
    \caption{Voxelization \texttt{cylinder.obj} (depth 4)}
    \label{fig:tc-cylinder}
\end{figure}

Silinder memiliki geometri lebih sederhana daripada sphere dan torus,
sehingga menghasilkan jumlah node aktif dan waktu eksekusi yang lebih kecil.

\subsection{Test Case 4}
\textbf{Model:} \texttt{teapot.obj}, \textbf{depth:} 4

\begin{figure}[H]
    \centering
    \begin{minipage}[c]{0.30\textwidth}
        \centering
        \includegraphics[width=\textwidth,height=4.2cm,keepaspectratio]{testcase/teapot_bef.png}\\[4pt]
        {\small (a) Mesh awal}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.30\textwidth}
        \centering
        \includegraphics[width=\textwidth,height=4.2cm,keepaspectratio]{testcase/teapot_aft.png}\\[4pt]
        {\small (b) Hasil voxelization}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.34\textwidth}
        \centering
        \small
        \begin{tabular}{lr}
            \toprule
            Metrik & Nilai \\
            \midrule
            Vertex awal & 530 \\
            Triangle awal & 1024 \\
            Voxel count & 418 \\
            Vertex output & 3344 \\
            Face output & 5016 \\
            Node total & 1129 \\
            Pruned total & 570 \\
            Waktu & 3.87 ms \\
            \bottomrule
        \end{tabular}
    \end{minipage}
    \caption{Voxelization \texttt{teapot.obj} (depth 4)}
    \label{fig:tc-teapot}
\end{figure}

Teapot menghasilkan voxel count paling kecil pada depth yang sama,
yang berarti distribusi permukaan objek relatif tidak memenuhi banyak sel leaf.

\subsection{Test Case 5}
\textbf{Model:} \texttt{torus.obj}, \textbf{depth:} 4

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.32\textwidth}
        \includegraphics[width=\textwidth]{testcase/torus_bef.png}
        \caption{Mesh awal}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.32\textwidth}
        \includegraphics[width=\textwidth]{testcase/torus_aft.png}
        \caption{Hasil voxelization}
    \end{subfigure}
    \hfill
    \begin{minipage}[b]{0.32\textwidth}
        \centering
        \small
        \begin{tabular}{lr}
            \toprule
            Metrik & Nilai \\
            \midrule
            Vertex awal & 512 \\
            Triangle awal & 1024 \\
            Voxel count & 752 \\
            Vertex output & 6016 \\
            Face output & 9024 \\
            Node total & 1865 \\
            Pruned total & 880 \\
            Waktu & 4.84 ms \\
            \bottomrule
        \end{tabular}
    \end{minipage}
    \caption{Voxelization \texttt{torus.obj} (depth 4)}
    \label{fig:tc-torus}
\end{figure}

Lubang pada torus menyebabkan pemetaan voxel yang khas, yaitu
lebih banyak voxel dibanding teapot, namun tetap lebih rendah dari cube/sphere
karena bagian volume tengah (void) tidak menambah permukaan terisi.

\subsection{Performance Summary}
Tabel berikut merangkum kelima pengujian utama.

\begin{table}[H]
    \centering
    \caption{Ringkasan performa seluruh test case (depth 4)}
    \begin{tabular}{lrrrrr}
        \toprule
        Model & Triangles & Voxels & Node Total & Pruned & Waktu (ms) \\
        \midrule
        cube.obj & 12 & 1352 & 2889 & 1176 & 4.34414 \\
        sphere.obj & 2208 & 1160 & 2697 & 1200 & 7.50067 \\
        cylinder.obj & 128 & 640 & 1801 & 936 & 3.94558 \\
        teapot.obj & 1024 & 418 & 1129 & 570 & 3.87233 \\
        torus.obj & 1024 & 752 & 1865 & 880 & 4.83732 \\
        \bottomrule
    \end{tabular}
\end{table}

Selain perbandingan antar-model, pengaruh depth juga diuji pada \texttt{cube.obj}.

\begin{table}[H]
    \centering
    \caption{Pengaruh depth terhadap jumlah voxel dan waktu (cube.obj)}
    \begin{tabular}{rrrr}
        \toprule
        Depth & Voxels & Node Total & Waktu (ms) \\
        \midrule
        2 & 56 & 73 & 1.67802 \\
        3 & 296 & 521 & 2.01615 \\
        4 & 1352 & 2889 & 4.34414 \\
        5 & 5768 & 13705 & 14.74950 \\
        \bottomrule
    \end{tabular}
\end{table}

Dari tabel depth terlihat bahwa penambahan resolusi meningkatkan detail,
tetapi mendorong kenaikan jumlah node dan voxel secara sangat cepat.
Nilai ini konsisten dengan sifat subdivisi octree ($\approx 8^d$ pada skenario terburuk),
meskipun pruning menekan pertumbuhan aktual agar tetap jauh di bawah batas teoritis.

Secara keseluruhan, implementasi mampu menjaga waktu eksekusi pada orde milidetik
untuk depth menengah (hingga depth 4) pada model uji yang beragam.

\section{Bonus Features}\label{sec:bonus}
\subsection{Concurrency}
Fitur concurrency diimplementasikan pada proses rekursif pembentukan octree
di \texttt{octree/Octree.cpp}. Ide utamanya adalah setiap child hasil pembelahan
node (8 sub-box) dapat diproses independen, sehingga secara alami cocok
untuk model \textit{task parallelism}.

Strategi yang dipakai:
\begin{itemize}
    \item Pada node yang masih memenuhi syarat paralelisasi,
    setiap child dieksekusi melalui \texttt{std::thread} terpisah.
    \item Setiap thread memanggil \texttt{buildOctree()} secara rekursif
    pada subtree masing-masing.
    \item Setelah semua thread dibuat, dilakukan \texttt{join()} untuk memastikan
    sinkronisasi sebelum fungsi parent selesai.
\end{itemize}

Karena seluruh thread menulis ke satu struktur hasil global (\texttt{OctreeResult}),
implementasi menambahkan proteksi \texttt{mutex} pada \textit{critical section} berikut:
\begin{itemize}
    \item update \texttt{nodesPerDepth},
    \item update \texttt{prunedPerDepth},
    \item penambahan voxel leaf ke \texttt{result.voxels}.
\end{itemize}

Implementasi saat ini memakai konstanta:
\begin{center}
\texttt{const int THREAD\_LIMIT = 1;}
\end{center}

Konfigurasi ini mengartikan bahwa kerangka paralel sudah tersedia,
tetapi secara default eksekusi dijaga stabil.
Jika \texttt{THREAD\_LIMIT} diperbesar, waktu eksekusi dapat meningkat
signifikan karena beberapa hal, seperti \textit{context switching}, perebutan
sumber daya (\textit{terlalu banyak thread membuatnya harus menunggu}),
\textit{thread creation overhead}, dan berbagai alasan lainnya.

Secara umum, manfaat utama fitur ini adalah:
\begin{itemize}
    \item Kode rekursif tetap modular
    \item Mekanisme sinkronisasi sudah siap
\end{itemize}

\subsection{Interactive 3D Viewer}

Setelah voxelization selesai dan file \texttt{.obj} sudah ter-generate, ada fitur viewer.
Hasil file \texttt{.obj} pada dasarnya bisa dibuka melalui Blender, tetapi cenderung overkill
untuk sekadar cek apakah bentuknya masuk akal.

Viewer ini dibangun menggunakan \textbf{raylib} (C++, cross-platform), dan
sifatnya fully interactive. Begitu proses voxelization dan export selesai, program langsung
membuka window baru yang menampilkan seluruh voxel hasil konversi dalam 3D space.

\subsubsection*{Camera System}

Kamera menggunakan model \textit{spherical orbit} yang mengelilingi pusat scene.
User bisa kontrol penuh lewat mouse. Selain itu, kamera juga bisa otomatis \textit{auto-orbit}
mengelilingi objek dengan kecepatan yang bisa diatur lewat slider (0 sampai 5$\times$ multiplier).

\subsubsection*{Render Modes}

Ada tiga mode render yang bisa di-cycle dengan key \texttt{[W]}:

\begin{enumerate}
    \item \textbf{Solid + Wireframe} (default). face solid dengan overlay wireframe semi-transparan.
    \item \textbf{Solid}. face solid tanpa wireframe jadi lebih clean.
    \item \textbf{Wireframe only}. hanya edge yang dirender, interior transparan.
\end{enumerate}

\subsubsection*{Cross-Section Clipping}

Fitur tambahan. Ini fitur yang mungkin paling berguna untuk analisis. Ada slider \textit{clip Y}
yang bisa digeser dari $-1.0$ sampai $1.0$, dan semua voxel di atas garis potong
itu langsung disembunyikan.

\subsubsection*{Explode View}

Dengan key \texttt{[E]}, semua voxel bisa di-``explode'' secara radial dari pusat
scene. Fitur ini sebenarnya tambahan juga dan berguna untuk melihat distribusi spasial
voxel secara individual, terutama di bagian yang biasanya tertutup voxel lain.


\section{Conclusion}\label{sec:kesimpulan}

Dari hasil pengujian pada lima model (cube, sphere, cylinder, teapot, torus) dengan
depth 4, program berhasil menghasilkan output voxel yang secara visual merepresentasikan
bentuk asli masing-masing objek. Waktu eksekusi berada di miliseconds untuk depth
menengah, yang menunjukkan bahwa pruning bekerja efektif. Pengujian pengaruh depth
pada \texttt{cube.obj} juga mengkonfirmasi trade-off antara resolusi dan biaya komputasi,
semakin tinggi depth, semakin detail hasilnya, tapi jumlah node dan waktu eksekusi
naik secara signifikan.

Dua fitur bonus juga berhasil diimplementasikan. Pertama, \textit{concurrency}
dengan \texttt{std::thread} dan proteksi \texttt{mutex} pada shared state, di mana
kerangka paralelisasi sudah tersedia meskipun secara default dijalankan konservatif
untuk menjaga stabilitas. Kedua, \textit{interactive 3D viewer} dengan raylib yang
menyediakan orbit camera, multiple render modes, cross-section clipping, dan explode
view, sehingga user bisa langsung memvalidasi hasil voxelization tanpa tool eksternal.

\begin{thebibliography}{9}

\bibitem{munir2026a}
R.~Munir, ``Algoritma Divide and Conquer (2026) --- Bagian 1,''
Teknik Informatika, Institut Teknologi Bandung, 2026.
\url{https://informatika.stei.itb.ac.id/~rinaldi.munir/Stmik/2025-2026/07-Algoritma-Divide-and-Conquer-(2026)-Bagian1.pdf}

\bibitem{munir2026b}
R.~Munir, ``Algoritma Divide and Conquer (2026) --- Bagian 2,''
Teknik Informatika, Institut Teknologi Bandung, 2026.
\url{https://informatika.stei.itb.ac.id/~rinaldi.munir/Stmik/2025-2026/08-Algoritma-Divide-and-Conquer-(2026)-Bagian2.pdf}

\bibitem{munir2026c}
R.~Munir, ``Algoritma Divide and Conquer (2026) --- Bagian 3,''
Teknik Informatika, Institut Teknologi Bandung, 2026.
\url{https://informatika.stei.itb.ac.id/~rinaldi.munir/Stmik/2025-2026/09-Algoritma-Divide-and-Conquer-(2026)-Bagian3.pdf}

\bibitem{munir2026d}
R.~Munir, ``Algoritma Divide and Conquer (2026) --- Bagian 4,''
Teknik Informatika, Institut Teknologi Bandung, 2026.
\url{https://informatika.stei.itb.ac.id/~rinaldi.munir/Stmik/2025-2026/10-Algoritma-Divide-and-Conquer-(2026)-Bagian4.pdf}

\bibitem{akenine2001}
T.~Akenine-M\"oller, ``Fast 3D Triangle-Box Overlap Testing,''
\textit{Journal of Graphics Tools}, vol.~6, no.~1, pp.~29--33, 2001.
\url{https://doi.org/10.1080/10867651.2001.10487535}
(Accessed: 18 March 2026).

\bibitem{schwarz2010}
M.~Schwarz and H.-P.~Seidel, ``Fast Parallel Surface and Solid Voxelization on GPUs,''
\textit{ACM Transactions on Graphics}, vol.~29, no.~6, article 179, 2010.
\url{https://doi.org/10.1145/1882261.1866201}
(Accessed: 18 March 2026).

\bibitem{baert2012}
J.~Baert, ``Out-Of-Core Construction of Sparse Voxel Octrees,'' 2012.
\url{https://www.forceflow.be/2012/07/24/out-of-core-construction-of-sparse-voxel-octrees/}
(Accessed: 26 March 2026).

\end{thebibliography}

\newpage
\section*{Repository}
\addcontentsline{toc}{section}{Repository}

\url{https://github.com/ethj0r/Tucil2_13524026_13524104}

\bigskip
\begingroup
\noindent{\Large\bfseries Lampiran}
\addcontentsline{toc}{section}{Lampiran}
\endgroup
\medskip

\begin{table}[H]
    \centering
    \caption{Checklist kelengkapan program}
    \begin{tabular}{clcc}
        \toprule
        \textbf{No} & \textbf{Poin} & \textbf{Ya} & \textbf{Tidak} \\
        \midrule
        1 & Program berhasil dikompilasi tanpa kesalahan & $\checkmark$ & \\
        2 & Program berhasil dijalankan & $\checkmark$ & \\
        3 & Hasil konversi \texttt{.obj} terdiri dari kubus-kubus kecil & $\checkmark$ & \\
        4 & Program menampilkan informasi-informasi atau report & $\checkmark$ & \\
        5 & Program dapat menerima masukan \texttt{.obj} dan mengembalikan output & $\checkmark$ & \\
        6 & {[Bonus]} Program menggunakan concurrency & $\checkmark$ & \\
        7 & {[Bonus]} Program dapat menampilkan visualisasi \texttt{.obj} & $\checkmark$ & \\
        \bottomrule
    \end{tabular}
\end{table}

\vspace{2cm}

\noindent
Tugas ini disusun sepenuhnya tanpa bantuan kecerdasan buatan (Generative AI),
melainkan hasil pemikiran dan analisis mandiri.

\vspace{2cm}

\noindent
\begin{minipage}[t]{0.45\textwidth}
    \centering
    \includegraphics[height=2.5cm]{assets/sign_Jor.png}\\
    \textbf{Made Branenda Jordhy}\\
    13524026
\end{minipage}
\hfill
\begin{minipage}[t]{0.45\textwidth}
    \centering
    \includegraphics[height=2.5cm]{assets/sign_Daniel.jpeg}\\
    \textbf{Valentino Daniel Kusumo}\\
    13524104
\end{minipage}

\end{document}